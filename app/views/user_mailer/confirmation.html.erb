<!DOCTYPE html>
<html>
<head>
	<style type="text/css">
		.wrapper {
			width: 300px;
			height: 150px;
			border-style: outset;
			background-color: ivory;
		}
		.gravatar-wrap {
			display: inline-block;
			width: 100px;
			padding: 10px;
			padding-top: 25px;
		}
		.gravatar {
			width: 100%;
			border: 1px solid darkgray;
		}
		.body {
			white-space: nowrap;
			display: inline-block;
			float: right;
			width: 170px;
			padding: 10px 0;
			font-size: 10px;
		}
		.name {
			margin-bottom: 5px;
			font-size: 18px;
		}
		.title {
			margin-top: 0;
			margin-bottom: 17px;
			font-weight: normal;
			font-size: 15px;
		}
		.phone, .email, .github {
			margin-bottom: 5px;
		}
		@media screen and (min-width: 600px) {
			.wrapper {
				width: 550px;
				height: 250px;
				margin-left: 30px;
			}
			.gravatar-wrap {
				width: 215px;
				padding: 15px;
			}
			.body {
				width: 290px;
				padding: 20px 0;
				font-size: 18px;
			}
			.name {
				margin: 7px 0;
				font-size: 1.8em;
			}
			.title {
				margin-bottom: 35px;
				font-size: 25px;
			}
			.phone, .email, .github {
				margin-bottom: 5px;
			}
		}
	</style>
</head>
<body>
	<p>
	  Hi <%= @current_user.name %>.
	  Your card has been sent to <%= @recipient.name %>.
	</p>

	<div class="wrapper">
		<div class="gravatar-wrap">
			<%= gravatar_for @current_user %>
		</div>
		<div class="body">
			<h1 class="name"><b><%= @current_user.name %></b></h1>
			<h2 class="title"><%= @current_user.title %></h2>
			<div class="phone"><%= @current_user.phone %></div>
			<div class="email"><%= @current_user.email %></div>
			<div class="github"><%= link_to @current_user.github, @current_user.github %></div>
			<div class="linkdin"><%= link_to @current_user.linkedin, @current_user.linkedin %></div>
		</div>
	</div>

	<p>Thanks for using CardShare</p>
</body>
</html>
