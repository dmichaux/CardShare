<% if user_signed_in? %>
	<%= link_to "Send a Card", new_recipient_path %>
	<%= link_to "Follow Up", recipients_path %>
	<%= current_user.name %> | 
	<%= link_to "Logout", destroy_user_session_path, method: :delete %>
<% else %>
	<%= render 'devise/sessions/new' %>
<% end %>
